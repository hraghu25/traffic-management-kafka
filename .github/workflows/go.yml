name: Go

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.21.4'

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v ./...

      - name: Read Version from manifest.yaml
        run: |
          VERSION=$(jq -r .version manifest.yaml)
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Create Tag
        run: |
          git tag -a "v${{ github.run_number }}" -m "Version ${{ github.run_number }}"
          git push origin "v${{ github.run_number }}"
        
      - name: Create Deployable Zip
        run: |
          zip -r "deployable_v${{ github.run_number }}.zip" ./path/to/build/output
